# Spring properties
spring:
  application:
     name: authorization-server  # Service registers under this name
  freemarker:
    enabled: false           # Ignore Eureka dashboard FreeMarker templates
#  thymeleaf:
#    cache: false             # Allow Thymeleaf templates to be reloaded at runtime
#    prefix: classpath:/authorization-server/templates/    # Trailing / mandatory


# Map the error path to error template
error:
  path=/error

# Discovery Server Access
eureka:
  instance:
    leaseRenewalIntervalInSeconds: 10
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/

# HTTP Server
server:
  port: 8080   # HTTP (Tomcat) port



#security:
#  oauth2:
#    resource:
#      userInfoUri: http://localhost:8080/uaa/user
#  user:
#    password: tutu



logging:
  level:
    org.springframework.security: DEBUG

#infra:
#  directorypath: c://Test
